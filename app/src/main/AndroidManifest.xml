<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.justin.perfect.royalestate">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SellBudgetAndPropertyTypeActivity"></activity>
        <activity android:name=".ContactDetailsActivity" />
        <activity android:name=".BuyBudgetAndPropertyTypeActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />

        <activity android:name=".GetOnRentActivity" />
        <activity android:name=".GiveOnRentActivity" />
        <activity android:name=".SellActivity" />
        <activity android:name=".BuyActivity" />
        <activity android:name=".OptionActivity" />
        <activity android:name=".OTPActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>